name: DevSecOps CI Pipeline - Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'apps/boutique/src/frontend/**'
      - '.github/workflows/**'

env:
  REGISTRY: ghcr.io
  # We will just use this for the Registry Login. 
  # For image tagging, we rely on the metadata action to normalize casing.
  IMAGE_NAME: ${{ github.repository_owner }}/boutique-frontend

jobs:
  build-scan-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: type=sha,format=short

      - name: Build Docker image locally
        uses: docker/build-push-action@v5
        with:
          context: ./apps/boutique/src/frontend
          load: true
          tags: ${{ steps.meta.outputs.tags }}

      # FIX: Use the 'tags' output which is guaranteed to be lowercase
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.meta.outputs.tags }}
          format: 'table'
          exit-code: '0' # Audit Mode
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL'

      - name: Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./apps/boutique/src/frontend
          push: true
          tags: ${{ steps.meta.outputs.tags }}

      - name: Update Kustomize Image Tag
        run: |
          # FIX: Use the normalized tag here too to ensure Kustomize gets the lowercase name
          NEW_IMAGE="${{ steps.meta.outputs.tags }}"
          echo "Updating to: $NEW_IMAGE"
          
          cd apps/boutique/overlays/dev
          kustomize edit set image gcr.io/google-samples/microservices-demo/frontend=$NEW_IMAGE
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions@github.com'
          git add kustomization.yaml
          git commit -m "chore: update frontend image to ${{ steps.meta.outputs.tags }} [skip ci]"
          git push
